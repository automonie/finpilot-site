# Sanity connection (NOT secrets — project id + dataset only grant read access to
# published content). Copy this file to `.env` and fill in your project id after you
# run `npx sanity init` / create the project at sanity.io/manage.
PUBLIC_SANITY_PROJECT_ID=replaceme
PUBLIC_SANITY_DATASET=production
